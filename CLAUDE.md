# CLAUDE.md

## プロジェクト概要

新卒就活生向けの音声面接練習アプリ。Next.jsフロントエンド＋Spring Boot/Spring AIバックエンドで、ローカルのOllama・faster-whisper・VOICEVOXを使い、音声で面接官と会話しながら練習できるようにする。ClaudeやChatGPTの音声会話UIをベースにした構成。

## 現在の開発フェーズ(最重要)

**練習モード・企業リサーチ・本番モード(本番模擬面接)ともにバックエンド・フロントエンドまで実装済み。**

- 対象:
  1. 練習モード: 1つの質問に対してAIがコーチとして会話するモード
  2. 企業リサーチ: 会社名 → Web検索 → AIが企業概要・面接傾向を要約 → 確認・編集 → AIが質問を一括生成、という会社追加ウィザード
  3. 本番モード(本番模擬面接): 質問ごとに独立した一問一答形式(計量モデルでは通しの会話進行が難しいと判断し、当初の6問組み合わせ・通し会話方式から作り替えた)。各質問のチャット画面右上の「本番を開始」ボタンから、その1問だけを対象に面接官として深掘り・進行 → 終了後にレポート(スコア・傾向分析・フィードバック)を生成。詳細は下の「本番モード」の決定事項を参照
  4. 汎用的な質問集: サイドバーの「会社」セクションの上に、会社に紐づかない汎用的な質問(既存の質問バンクを複製したもの)を表示する。練習・本番とも会社の質問と全く同じ仕組みで動く(詳細は下の決定事項を参照)
- 企業リサーチのWeb検索は `WebSearchClient` インターフェースの背後に閉じ込める。実装は自己ホストのSearXNG(`SearXngWebSearchClient`、APIキー不要、`docker-compose.yml`の`searxng`サービス)。リサーチ結果は `companies.overview` に集約する(`company_interview_steps` / `company_review_summaries` テーブルは今は使わない)
- 対象外(今は着手しない): サイドバーのカテゴリ表示(内部カテゴリをUI上のフォルダとして作らない、詳細は「質問の並び」参照)
- ここに書かれていない仕様変更の依頼が来ても、既存コードを壊さないことを優先する。設計に影響する大きな変更が必要な場合は、実装前に必ず確認する

## 技術スタック

- Backend: Java 21 / Spring Boot 3.x / Spring AI (ChatClient, `@Tool`, `.entity()`)
- Frontend: Next.js (App Router) / TypeScript / React
- LLM: Ollama (**既定はDockerではなくホスト側でネイティブ起動**する運用。Mac: `brew install ollama && ollama serve`。Windows: [ollama.com](https://ollama.com/download/windows) からインストーラーで導入するとバックグラウンドサービスとして自動起動する(手動で`ollama serve`を叩く必要はない)。devcontainerからは `OLLAMA_BASE_URL=http://host.docker.internal:11434` でホストのOllamaに到達する(`docker-compose.dev.yml`が既定でこの値を設定する。`host.docker.internal`はDocker Desktop for Mac/Windowsのどちらでも解決できる)。日本語の自然さを優先し、既定モデルは `qwen3.5:4b`(アプリのデフォルト値としてコードに設定済み。`OLLAMA_MODEL`未指定時のフォールバック)。Docker上でCPU動作させる等メモリに余裕がない環境では `OLLAMA_MODEL` を `qwen3.5:2b`/`qwen3.5:0.8b` に落とす、GPUに余裕がある環境では `qwen3.5:9b` に上げる。**CPUが非力でNVIDIA GPUがあるWindows機**(例: RTX 3060)向けに、`docker-compose.yml`/`docker-compose.dev.yml`に`ollama`サービスをprofile(`ollama-gpu`)として用意している。`docker compose --profile ollama-gpu up -d`で起動でき、ホストのポート11434に公開されるためネイティブ起動と同じ`OLLAMA_BASE_URL`設定で到達できる。既定では起動しないので他環境の運用に影響しない。コンテナ内でモデルをpullする場合は `docker compose --profile ollama-gpu exec ollama ollama pull qwen3.5:9b` を実行する。devcontainerには`docker-outside-of-docker`機能(`.devcontainer/devcontainer.json`)を入れており、devcontainer内のターミナルからでも`docker`/`docker compose`コマンドでホストのDockerデーモンを操作してこのコンテナを起動できる)
  - **RTX 3060(12GB VRAM)向けの推奨モデル**: `qwen3.5:9b`(量子化済みファイルサイズ約6.6GB)が、12GB VRAMにフルロードしつつ`num_ctx`(8192)分のKVキャッシュ余裕も確保できる、実質的な上限ライン。次のサイズの`qwen3.5:27b`はファイルサイズだけで約17GBあり12GB VRAMには収まらない(CPUオフロードすれば動かせなくはないが、本アプリはリアルタイム音声対話+TTSパイプラインを前提にしており低レイテンシが最優先のため、GPUに収まらないモデルは推奨しない)。したがって`qwen3.5:4b`(既定)で応答の質に不満がある場合の次の一手は`qwen3.5:9b`であり、それ以上のサイズはRTX 3060では現実的でない。`.env`等で`OLLAMA_MODEL=qwen3.5:9b`を設定し、`ollama pull qwen3.5:9b`(または`ollama-gpu`プロファイル利用時は上記のexecコマンド)でモデルを取得する
- STT: faster-whisper (Dockerコンテナ、REST経由で呼び出す)
- TTS: VOICEVOX Engine (Dockerコンテナ、REST経由で呼び出す)
- DB: PostgreSQL (Docker Compose。開発初期はH2でも可、本実装はPostgres前提でスキーマを書く)
- リアルタイム通信: WebSocket (マイク音声の送信・AI応答テキスト/音声の受信)

## ディレクトリ構成

```
backend/
  src/main/java/com/interviewapp/
    question/    質問関連 (InterviewQuestion)
    session/     チャットセッション (練習/本番共通の基盤)
    practice/    練習モード固有のロジック
    chat/        WebSocketハンドラ、STT/TTS連携
frontend/
  app/                Next.js App Router
  components/
    chat/             チャットUI・録音ボタンなど
  lib/
    websocket.ts       WebSocketクライアント
docs/
  interview_app_db_api_design.md   DB/API設計書(参照用、@import対象)
```

DB/APIの詳細設計は `docs/interview_app_db_api_design.md` を作成済みなので、`docs/`配下に配置してから以下を参照すること。

@docs/interview_app_db_api_design.md

## ビルド・テストコマンド

- Backend起動: `./gradlew bootRun` (Windowsは `gradlew.bat bootRun` / PowerShellでは `.\gradlew.bat bootRun`)
- Backendテスト: `./gradlew test` (コミット前に必ず実行。Windowsは `gradlew.bat test`)
- Frontend起動: `npm run dev`
- Frontendビルド確認: `npm run build`
- Frontend Lint: `npm run lint` (コミット前に必ず実行)
- Frontendユニットテスト: `npm run test` (Vitest。コミット前に必ず実行)
- FrontendE2Eテスト: `npm run test:e2e` (Playwright。機能追加時に随時実行)
- ローカルサービス一括起動: `docker compose up -d` (faster-whisper / VOICEVOX / DB)。**Ollamaはこの対象外**なのでホスト側で別途起動しておくこと(Macは `ollama serve`、WindowsはOllamaインストーラーが常駐サービスとして自動起動するため通常は不要)

## アーキテクチャ上の決定事項(変更する場合は要相談)

- **音声ファイルは保存しない。** STTでテキスト化した結果のみをDBに保存する(`chat_messages`に音声用カラムは持たない)
- **練習モード**: 「適切な回答を一緒に練り上げる相談役」に特化し、本番モードと明確に差別化する。AI側から会話を終了してはいけない。ユーザーが終了ボタンを押すまで継続する。ユーザーが回答に詰まったら、すぐに具体的なコツや回答例を提示する。終了時のスコアリングは不要(軽いサマリーのみ任意で可)
  - **面接官風の連続した深掘り質問はしない**(それは本番モードの役割)。毎ターン「①良い点と改善点を1つ→②必要なら結論→理由→具体例の型で構成を整理→③その場で言える回答例を積極的に提示」を軸に、回答の中身を一緒に良くしていくことに徹する。ユーザーの状況が分からず一緒に回答を作れないときだけ、短い確認の質問を1つだけする。プロンプトは`PracticePromptFactory`のみで制御しており(深掘り打ち切りのようなコード側のロジックは無い)、回答内容の妥当性チェック(会社情報との整合・具体的根拠)・STAR/PREPの型・回答例の捏造ガード・話し方メモの扱い・`<<ADVICE>>`マーカーはそのまま維持している
- **練習モードのメッセージ種別**: AIが「アドバイス・回答例」を出す場合は応答冒頭に`<<ADVICE>>`マーカーを付けさせ、バックエンドで検出して`chat_messages.message_type`に反映する(マーカーが無ければ`NORMAL`)。本番モードの深掘り終了検知(`<<QUESTION_DONE>>`)と同じマーカー方式に統一する。WebSocketでは`assistant_message_start`イベントの`messageType`でフロントに伝える
- **本番モード(バックエンド・フロントとも実装済み)**: `com.interviewapp.mock`パッケージ。質問ごとに独立した一問一答形式(前述)のため、開始導線はサイドバー固定ボタンではなく**各質問の練習チャット画面(`ChatPanel.tsx`)のヘッダー右上に置いた「本番を開始」ボタン**(`onStartMock`)。押すと`POST /api/questions/{questionId}/mock-sessions`を叩き、`MockSessionService.startSession`が対象の`interview_questions`1件をそのまま`mock_session_questions`(そのセッション専用、会社に紐づく静的な`interview_questions`とは別)に1件だけコピーする(LLM呼び出しは無く、ほぼ即時に開始できる)。以前はサイドバー固定の「本番開始」ボタン→会社選択モーダル→自己紹介固定+質問バンクの汎用質問2問+LLM生成の会社特有質問3問を組み合わせた計6問、という会社単位の通し会話方式だったが、計量モデルでは通しの会話進行が難しいと判断し全面的に作り替えた(この経緯・教訓は次のサブ項目群を参照)。フロントは`useMockSession`(`ActiveMockQuestion`が対象の質問を保持)+`MockInterviewPanel`/`MockSummaryScreen`をそのまま流用し、`page.tsx`が`mock.phase`に応じて練習チャットの代わりに表示を切り替える(画面遷移やモーダルは無い)
  - **計量(小型)ローカルモデルでは通しの会話進行(挨拶・相槌・次の質問への自然なつなぎ)を安定させるのが難しいと判断し、「質問ごとに独立した一問一答形式」に作り替えた**(元々は本番全体を1つの通し会話として進行させ、面接官が挨拶→自己紹介依頼→深掘り→次の質問への滑らかな移行を全てLLMに任せていたが、下記のように小型モデルが指示に従わない不具合が繰り返し発生したため、既存の通し会話実装は撤廃した)。質問の提示・切り替えは常にコード側が`mock_session_questions`の文面をそのまま話す(LLM呼び出しなし。挨拶・相槌・つなぎの生成をやめた)。LLMに任せるのは、今の質問について「まだ聞くか・十分か」を判断しながら深掘りの質問を1つずつ重ねることだけに絞り、渡す会話履歴も常に今の質問に属するやり取りだけに絞り込む(`MockTurnService.historyForCurrentQuestion`、質問をまたいだ話題の蒸し返しを構造的に防ぐ)。十分だとLLMが判断したら応答全体を`<<QUESTION_DONE>>`マーカーだけにして返す(相槌や次の質問文は書かせない)。マーカーを検出したら、次の質問があればその文面をそのまま話し、無ければ固定の締めくくり文言(`MockInterviewPromptFactory.INTERVIEW_CLOSING_MESSAGE`)を話して面接を終える(次の質問があるかどうかはコード側で確定的に判定するため、モデルは「この質問が十分か」だけを判断すればよい)。自己紹介の固定質問文(`SELF_INTRODUCTION_QUESTION`)にも簡単な挨拶を含め、静的な文言だけで自然な出だしにした(以前はLLMが冒頭ターンで挨拶・名乗り・自己紹介依頼を生成していたが、これも撤廃した)。以下は、この設計にたどり着くまでの試行錯誤の記録
  - **質問構成を「自己紹介+汎用質問2問(会社非依存)」対「会社特有質問3問」でおよそ半々にした**: ユーザーから「会社が違っても志望者自身のことを問う質問が面接の半数を占める」という指摘があり、以前は5問すべて(自己紹介以外4問)を毎回LLMにその場で生成させていたのを、汎用的な「志望者自身のこと」を問う質問(挫折経験・長所短所・就活の軸・小中高から現在までの自己紹介など)を`question_bank_entries`に追加登録(`V5__add_mock_generic_questions.sql`、新規カテゴリ`JOB_AXIS`を含む)し、`QuestionBankService.sampleGenericForMock`でランダムに複数ピックアップして使い回す方式に変更した。会社特有の質問はLLM生成のまま3問(志望動機・技術/事業関連・逆質問)に絞り、会社選択は「質問全部を作り直す」役割から「少しの会社特有質問の生成とフィードバック時の文脈」という役割に縮小した。汎用質問のカテゴリはMOTIVATION(志望動機)/REVERSE_QUESTION(逆質問)を除く(会社色が強いカテゴリのため)SELF_PR/EXPERIENCE/JOB_AXISの3カテゴリからサンプリングする
  - **フロントは練習モードとは別の`useMockSession`フック＋専用コンポーネント一式**: `MockInterviewPanel.tsx`(面接画面。進捗`質問X/Y`と現在の質問文をヘッダーに表示)・`MockSummaryScreen.tsx`(結果画面。スコア・傾向分析・質問ごとのフィードバック・過去の挑戦一覧)。`ChatPanel`と共通の`MessageList`/`MicButton`/`Composer`/`WaveBars`/`audioQueuePlayer`/`sentenceSplitter`/ライブキャプションのロジックはそのまま再流用し、バックエンドと同じく「新しく作るのは差分だけ」という方針を踏襲した(`MicButton`だけ`respondingLabel`propを追加し、本番では「面接官が応答中...」と表示できるようにした)
  - **WebSocketクライアントも`PracticeSocket`を1クラスのまま両モードで共用**: バックエンドが同じエンドポイント・プロトコルを両モードで使うのに合わせ、`onMockQuestionAdvanced`/`onReportReady`ハンドラを追加しただけ(練習モードでは呼ばれないだけ)。クラス名は`PracticeSocket`のままだが、Javadoc的なコメントで両モード共用である旨を明記した
  - **`app/page.tsx`は`usePracticeSession`と`useMockSession`を両方常時マウントし、どちらのphaseが"selecting"でないかで表示を切り替える**(片方を開始する際はもう片方を`reset()`してから始め、常にどちらか一方だけがアクティブになるようにする)。将来的にモードを増やすなら、この場当たり的な二重マウント方式ではなく共通の「アクティブなモード」ステートマシンへリファクタリングする余地がある
  - **ユーザー強制終了時のレポート取得はポーリング**: AI判断による終了(最後の質問で`<<QUESTION_DONE>>`が出た場合)はサーバーがWebSocketを開いたまま`report_ready`を送ってくるので即座に取得できるが、ユーザーが「終了する」を押した場合は`POST /end`が202を返すだけで完了を知らせるWSイベントが無いため、`useMockSession`が`GET /report`を2秒間隔・最大30回(約1分)ポーリングする。それでも取得できなければエラー表示に切り替える
  - **1問の深掘り生成は練習モードと同じ`PracticeCoachLlm`をそのまま再利用する**(汎用のチャットストリーミング抽象で本番/練習固有のロジックを持たないため)。本番固有なのはプロンプト(`MockInterviewPromptFactory.followUpSystemPrompt`、面接官ペルソナでコーチのような助言はしない)と、深掘り終了マーカーの解釈(`MockTurnMarkerParser`)だけ。質問の提示・切り替え自体はLLMを呼ばない(前述)
  - **面接官から話し始める**: WebSocket接続時(`status`が`READY`)に自動で`MockTurnService.startInterview`が走り、最初の質問(自己紹介の固定文言)を発話する(練習モードのようにユーザーが先に答えるのを待たない、実際の面接に近い体験にするため。LLM呼び出しは行わない)
  - **1問あたりの深掘りの打ち切り**: AI判断(応答全体を`<<QUESTION_DONE>>`マーカーだけにする、`<<ADVICE>>`と同じマーカー方式だが、こちらは応答全体がマーカーそのものになる点が異なる)＋フォローアップ上限`MockTurnService.MAX_FOLLOWUPS_PER_QUESTION`(既定5回)の組み合わせ。小型ローカルモデルが「十分」と判断しない/し忘れるケースに備えた安全策で、上限に達した場合はLLMを呼ばずに強制的に次の質問へ進める(下記の教訓を踏まえ、上限到達時はモデルへの問い合わせ自体を省略する設計にした)
  - **終了判定は「AIが十分と判断」(最後の質問で`<<QUESTION_DONE>>`が出た、次の質問が無いとコード側が判定した場合)または「ユーザー強制終了」の2系統**(`EndedReason.AI_JUDGED`/`USER_ENDED`)。終了後にバックグラウンドで1回のLLM呼び出しにまとめてスコア・具体的フィードバック・傾向分析を生成する(`MockSessionService.triggerReportGeneration`、質問ごとの並列評価はしない)。AI判断による終了はWebSocketハンドラが直接非同期トリガーし、`report_ready`イベントを同じ接続に送ってから閉じる。ユーザー強制終了は`POST /api/sessions/{sessionId}/end`が202を返しつつ非同期トリガーし、結果は`GET /api/sessions/{sessionId}/report`で取得する(未生成/生成中は404)。`triggerReportGeneration`は`mock_interview_reports`の存在チェックで冪等にしてあり、両方の経路から呼ばれても二重生成しない
  - **質問ごとの回答をレポート生成に渡すため`chat_messages.question_order`を追加**: 各メッセージがどの質問(`mock_session_questions.display_order`)への回答/深掘りだったかを記録する(PRACTICEでは常にnull)。レポート生成時にこれでグルーピングし、質問ごとの書き起こしをLLMに渡す
  - **レポート生成もタイムスタンプ付きの厳密フォーマット(`SCORE:`/`TENDENCY:`/`Q1:`/`A1:`/`F1:`...)で出力させ、`SpringAiMockInterviewLlm.parseReport`でパースする**(質問生成の行パースと同じ考え方。フィードバックが欠けた質問は結果から除く、スコア・傾向分析のどちらかが欠けたら失敗として扱う)
  - **レポートのフィードバックも、話し方だけでなく回答内容の妥当性(会社情報との整合性・具体的根拠の有無)を重視する指示にしている**(練習モードの同種の修正と同じ方針)
  - **会社情報との整合性チェックはレポート生成時のみ行い、1ターンごとの面接官応答には使わない**: 初版の`interviewerSystemPrompt`(現`followUpSystemPrompt`)は練習モードの「回答内容の妥当性を確認する」指示をそのまま流用し、深掘りのたびに「会社情報と整合しているか」を意識させていたが、これが原因で候補者が自己紹介で話した具体的な内容(取り組んでいる活動など)を無視して「その姿勢は当社の価値観に共感する」のような唐突で不自然な相槌を返す不具合が実際に発生した。修正として、1ターンの深掘りは**候補者が今話した内容そのもの(固有名詞・具体的な取り組み)を掘り下げることを主軸**にする指示に変更し、会社情報は「この1ターンの評価基準には使わない」と明記した(会社情報はプロンプトに残すが、あくまで志望動機等の話題選びの背景情報にとどめる)。整合性の評価は`reportSystemPrompt`(面接後のレポート生成)側にだけ残している。**教訓**: 練習モード(コーチ役、都度フィードバックする)向けに書いた指示を、ペルソナも会話の目的も違う本番モード(面接官役、フィードバックは最後にまとめて)にそのまま転用すると、文脈に合わない指示になりうる
  - **自己紹介(1問目)はLLM生成に任せず固定文言にする**: 質問フロー生成をLLMに5問(自己紹介込み)任せていたところ、自己紹介の質問文が疑問文ですらない不自然な文(会社の話をいきなり始める等)になる不具合が実際に発生した。面接の出だしという最も重要な部分の安定性を優先し、`MockInterviewPromptFactory.SELF_INTRODUCTION_QUESTION`(固定文言「簡単に自己紹介をお願いします。」)を`MockSessionService.startSession`がフローの先頭に差し込む形に変更した(現在はさらに質問バンクの汎用質問2問も加わり、LLMには会社特有の3問だけを生成させる。前述の質問構成の変更を参照)
  - **継続ターンの相槌が候補者の経歴・学校・所属への感想や評価になってしまう不具合を修正**: 「会社情報を評価基準に使わない」対策(前述)をしても、面接官がAIとして候補者の話した学校・環境そのものについて「〜は強みだと感じます」のような評価・感想を述べてしまう不具合が別途発生した(実例:「非常に実践的かつ本格的なカリキュラムを受けた環境で学ばれた背景は…大きな強みだと感じます」)。原因は継続ターンの指示が「自然に反応してから話を進めます」とだけで、相槌の中身を具体的に制約していなかったこと。「まず短い相槌(1フレーズ)→深掘りの質問」という型を明示し、「面接官は評価や感想を言う立場ではなく、聞き出す立場です」と役割を明確化したうえで、実際に発生した不具合に近い悪い例と、短い相槌+具体的な質問の良い例を`interviewerSystemPrompt`(現`followUpSystemPrompt`)に追加した。**教訓**: 「〜しない」という抽象的な禁止(会社情報を評価基準に使わない等)を一箇所直しても、同じ根本原因(面接官がコーチ的に評価・感想を述べてしまう)が別の言い回しで再発しうる。具体的な型(テンプレート)と良い例/悪い例のペアで縛るほうが小型モデルには効果的
  - **「短い相槌→質問」という型にしてもなお学校の解説を始めてしまう不具合が再発したため、ユーザー指定のテンプレートに沿って深掘りプロンプトを書き直した**: 相槌の中身を「短い相槌」とだけ指定しても、その相槌が長い解説になりうることまでは縛れていなかった。「候補者が話した内容を短く引用(『〇〇に取り組んでいらっしゃるとのことですが、』)→その意味や意図・経験したことを引き出す質問」という型に変更し、「感想や評価」だけでなく「説明・解説(学校がどんな場所か、専門用語の意味の説明等)」も明示的に禁止した。面接官自身の経歴(会社に入った経緯等)を語る一節は将来の拡張ポイント(面接官プロフィール機能)として想定しているが、今回は実装していない(でっち上げた経歴を毎回変わる形で語らせると不自然さ・一貫性の無さにつながるため)。なお、冒頭ターンのお礼・名乗り・自己紹介依頼は、後の一問一答形式への作り替え(前述)で固定文言化し、LLM生成自体をやめた
  - **フォローアップ上限に達しても、内部の質問インデックスだけを進めて実際にAIが話す内容には何も指示していなかったため、上限到達後も同じ話題への深掘りが積み上がる不具合があった**: 対策として一時期は上限到達時のターンだけ「深掘りを続けてよい」という選択肢自体をプロンプトから取り除く方式にしたが、後述のとおり最終的には上限到達時はLLMを呼ばずコード側で確定的に次へ進める設計に切り替え、この問題自体を構造的に解消した。**教訓**: 「上限に達したら内部状態だけ強制的に進める」という実装は、モデルへの指示が変わらなければモデルの出力(実際の発話)は変わらない。状態遷移とモデルへの指示は必ずセットで変える
  - **画面上部の「現在の質問」は正しく進行しているのに、実際の深掘り内容は最初の質問(自己紹介)の回答に永遠に引きずられる不具合を修正**: 上記までの対策で質問インデックスの進行自体(`mock_question_advanced`イベント、ヘッダー表示)は正しく動いていたが、`MockTurnService.handleUserTurn`が`toHistory`に渡す会話履歴は**セッション開始からの全メッセージ**だった。これが原因で、2問目以降の深掘りでも、LLMは毎ターン自己紹介の濃いやり取り(自己紹介はやり取り自体が多く内容も詳しい)を含む全履歴を見ることになり、「深掘りの主軸は候補者が今話した内容そのもの」という指示があっても、どのターンの発言を指すかが曖昧なため、小型モデルが会話の中で一番印象に残っている(=自己紹介の)話題に何度も戻ってしまっていた。プロンプト指示だけで縛るのではなく、既にレポート生成用のグルーピングに使っていた`chat_messages.question_order`を使い、LLMに渡す履歴自体を**今の質問に属するやり取りだけ**に絞り込む(`MockTurnService.historyForCurrentQuestion`)よう修正した。質問が切り替わった直後は絞り込み後の履歴が空になり、直前の質問の内容がLLMのコンテキストに一切現れなくなるため、話題の蒸し返しが構造的に起きなくなる(レポート生成は引き続き全件を扱うため影響しない)。**教訓**: 話題が逸れる/戻る系の不具合は、プロンプト指示(「今の内容だけを見て」)で縛るより、そもそも見せる情報(コンテキスト)自体を絞る方が小型モデルには確実。今回は`question_order`という既存のデータ構造がそのまま使えたため、大きな設計変更なしに対応できた
  - **履歴を絞り込んでも、モデルが「反応した上で次の質問Xを一字一句相当の内容で提示する」という複合的な指示にはうまく従わず、マーカーだけ付けて内容は元の話題の深掘りのまま、といった応答が再発したため、最終的に質問の提示・切り替えを丸ごとモデルから切り離した**: これはプロンプトの文言をさらに調整しても再発しうる、小型モデルの複合指示への根本的な追従性の弱さに起因する問題と判断し、モデルへの依存をさらに減らす方向で対応した結果、冒頭で述べた「質問ごとに独立した一問一答形式」への作り替えに至った。今はモデルの短い反応と次の質問の文面を連結する処理自体を廃止し、深掘りを終える判断(`<<QUESTION_DONE>>`マーカー)と、次に何を話すか(次の質問の原文、またはコード側で確定的に判定)を完全に分離している。**教訓**: 同じ「モデルに従ってほしい」バグがプロンプト修正を重ねても形を変えて再発する場合、それは「もっと強く指示する」問題ではなく「その仕事をモデルにやらせるべきではない」というシグナル。システム側が既に正解を知っている情報(次に何を話すべきか)は、モデルに生成・引用させず、コードで機械的に組み込む方が確実
  - **継続ターンが、候補者が既に話した理由を無視して同じ「なぜ」を聞き返したり、複数の話題をまとめて面接官が因果関係を推測する不自然な分析的言い回し(「〜という具体的情報が、なぜ〜」等)になったりする不具合を修正**: 実際の自己紹介で「Claudeの音声チャットの利用制限にすぐかかってしまうので、無料で使いたくて自分で面接練習アプリを作った」という経緯を候補者が既に自分から話しているのに、AIが「その『使用制限がかかっていた』という具体的情報が、なぜ開発意欲を強くしたのか気になります」と、既に説明済みの理由をまるで聞いていなかったかのように問い直す不具合が発生した。原因は、①「経緯や理由を既に話していれば別の角度から深掘りする」という指示はあったが、それに反して同じ理由を聞き返さないための具体的な縛りが無かったこと、②「短く引用」の指示はあっても、複数の話題(この例ではAtCoderでの学習と使用制限の話)を1つの分析的な文にまとめ、候補者が言っていない因果関係を面接官が勝手に推測して付け加えることまでは禁止していなかったこと。対策として、`openingOrContinuingInstruction`(現在は`followUpSystemPrompt`に統合)の継続ターン向けテンプレートに①候補者の発言を1つだけ短く取り上げ因果関係を推測しない指示、②理由を既に話している場合は同じ「なぜ」を繰り返さず別の角度(取り組み方・苦労・期間・規模等)を尋ねる指示、③候補者の発言を「情報」や「内容」として客観的に分析するような言い方(「〜という具体的情報が」等)を避ける指示を追加し、実際に発生した不具合に近い悪い例/良い例のペアを添えた。**教訓**: 「経緯を既に話していれば別角度に」のような条件付き指示だけでは、小型モデルは会話履歴の中の該当箇所を正しく参照できず条件判定そのものを誤ることがある。誤りが実際に起きたパターンを具体的な悪い例として明示するほうが確実
- **質問の並び**: サイドバーは会社の直下に具体的な質問がそのまま並ぶ。カテゴリ(自己PR/志望動機など)はUI上のフォルダとしては作らず、`interview_questions.internal_category` に内部タグとしてのみ持たせる
- **汎用的な質問集(会社に紐づかない質問)**: サイドバーの「会社」セクションの上に、既存の質問バンク(`question_bank_entries`)の内容を複製した専用の質問一覧を表示する(会社と同じくプルダウンで開閉できる)。`interview_questions.company_id`がNOT NULLかつ`companies`へのFKであること、`chat_sessions`(練習・本番とも)が会社単位で進行の文脈(会社概要等)を持つことなど、既存の仕組みがどこも「会社に紐づく」ことを前提にしていたため、ゼロから会社非依存の経路を作るのではなく、**固定UUIDの特別な「会社」を1行だけ作り(`companies.is_generic`、`V7__add_generic_question_set.sql`)、質問バンクの内容をその会社の`interview_questions`としてそのまま複製する**方式にした。これにより練習・本番(一問一答)・レポート生成など既存の仕組みを一切変更せずに再利用できる(`CompanyService.list`は`is_generic=false`のみを返し通常の会社一覧からは除外、`GET /api/companies/generic`で専用に取得する)。質問バンク自体は企業リサーチの質問生成プロンプトの手本(`QuestionBankService.sampleForPrompt`)としても引き続き使うため、テーブルは分けたまま複製する(複製後のライブ同期はしない、質問バンクと同じくキュレーションした静的データという位置づけのため)。この特別な会社は`CompanyService.delete`で削除できないようガードしている。**カテゴリはSELF_PR/EXPERIENCE/JOB_AXISのみ**にする(志望動機`MOTIVATION`・逆質問`REVERSE_QUESTION`は「当社」を前提にした会社ごとに回答が明確に変わる質問なので`V8__trim_generic_question_set.sql`で除外した。旧`QuestionBankService.sampleGenericForMock`が同じ3カテゴリだけを対象にしていたのと同じ整理)
- **企業リサーチ**: Web検索は `WebSearchClient` 抽象の背後に閉じ込め、実装は自己ホストのSearXNG(`SearXngWebSearchClient`)。会社追加ウィザードはステートレスなAPI(`POST /api/companies/research`、`POST /api/companies/generate-questions`)で未保存の下書きを作り、最終ステップの `POST /api/companies`(`questions[]` 付き)で会社＋質問を一括作成する(途中でキャンセルしても孤児レコードを残さない)。LLMによる要約・質問生成は `CompanyResearchLlm` 抽象の背後に置く(`PracticeCoachLlm` と同じ方針)。Ollama呼び出しはSpring AIのChatClientではなく `com.interviewapp.llm.OllamaChatClient` (自前のWebClientラッパー、`/api/chat` を直接叩く)経由。理由: Qwen3系のようなハイブリッド思考モデルは既定でthinkingを行い、使用中のSpring AI(1.0.0-M5)が `think` フィールドを露出していないため空応答になる問題があり、それを回避するため。リサーチ・質問生成の呼び出しは非ストリーミングで、`options`(`num_predict`等)で出力トークン数を絞る。質問生成は会社概要だけでなく `question_bank_entries`(定番の質問パターン+回答の型、`QuestionBankService`がカテゴリごとにサンプリング)も手本として渡し、質問が会社概要の言い回しに引っ張られて過度に狭くなるのを防ぐ
- **企業リサーチの自動Web検索の安全策**: SearXNG経由で見つけたURLを`UrlContentFetcher`(Jsoup)で直接スクレイピングすることになるため、法的・倫理的リスクを下げる目的で複数の安全策を入れている。`RobotsTxtChecker`でrobots.txtの`Disallow`/`Crawl-delay`を尊重(取得失敗時はブロックしない)、`HostFetchThrottle`で同一ホストへの連続アクセスに最低間隔を強制、`CompanyResearchService`に自動検索全体のクールダウン(既定10秒)、fetch自体には`SOURCES_FETCH_TIMEOUT_MS`のタイムアウトを設定。自動追加するソース件数も`AUTO_SOURCE_LIMIT`(既定3件)で絞っている
- **練習モードのTTS遅延対策**: VOICEVOXの`audio_query`(軽い・テキスト解析)と`synthesis`(重い・音声合成)を分離し、文が確定した時点で`audio_query`だけ即座に並行実行(`audioQueryExecutor`)、`synthesis`は単一エンジンへの同時リクエストを避けるため単一スレッドで直列化(`synthesisExecutor`)する構成にしている(`PracticeWebSocketHandler`)。加えて`app.voicevox.speed-scale`(既定1.1倍)で読み上げ速度を上げ、`TtsCache`で同一文・同一話者の合成結果を再利用する。VOICEVOX Engine自体のネイティブストリーミング合成は2026年時点でまだ設計段階で未実装のため(`voicevox_engine` issue #1492)、WAVを1文単位で待って送る方式が現状の上限
- **練習モードのテキスト表示は音声に同期させる(AIの発話のみ)**: AIの発話テキストは、対応する文の音声再生が実際に始まったタイミングでチャット吹き出しに表示する(フロントの`usePracticeSession.ts`が文ごとに`pendingSentencesRef`に溜め、`audioQueuePlayer`の`enqueue`第2引数`onStart`コールバックで表示する)。**注意**: 表示先のメッセージIDは、音声到着時点(`onAssistantAudio`が呼ばれた瞬間)で`streamingIdRef.current`をクロージャに固定して使う。実際の再生開始(`onStart`発火)はそのずっと後になりうるため、再生時に`streamingIdRef.current`を読み直すと既に次ターン用にnull化/上書きされていて表示が失われるバグを一度踏んだ(音声は最後まで再生されるがテキストが途中で欠ける症状だった)。TTS合成に失敗して音声が来なかった文は`assistant_message_end`受信時にまとめて表示するフォールバックがある。吹き出し内には`streaming`中、本文がまだ空なら「生成中...」、本文が表示され始めたら「再生中」を出す(`MessageBubble.tsx`)。AIの応答はマイクボタン上のライブキャプションには出さない(同じ内容を2箇所に同時表示するのをやめた)
- **ライブキャプションUI(自分の発話のみ)**: `ChatPanel.tsx`のマイクボタン上に、文字起こし結果を即時(演出的な遅延なし)表示する(`data-testid="live-caption"`)。確定した文字起こし結果は既に全文確定しているため小分けに見せる意味が無く、速度優先でそのまま出す。AIの応答が始まったタイミング(`onAssistantStart`)でこのキャプションは消える。確定済みのやり取りは従来通り`MessageList`のチャット履歴に残る
- **テキスト入力(`Composer.tsx`、練習・本番共通)は複数行に対応**: 元は単一行の`<input>`だったが、複数行入力したいという要望を受けて自動リサイズする`<textarea>`(`rows=1`から`scrollHeight`に応じて最大160pxまで伸びる)に変更した。Enterで送信、Shift+Enter(またはIME変換確定中のEnter、`e.nativeEvent.isComposing`で判定)で改行する、一般的なチャットUIの操作感に合わせている
- **録音中のリアルタイム部分文字起こし(本格的なストリーミングSTT)**: 録音停止(`end_turn`)を待たず、録音中も数秒おき(既定2秒間隔、フロントの`usePracticeSession.ts`の`setInterval`)にその時点までの音声バッファをプレビュー文字起こしし、マイクボタン上のキャプションを更新する。プロトコルはWSメッセージ`request_partial_transcript`(クライアント→サーバー)/`partial_transcript`(サーバー→クライアント、`WsProtocol`/`WsEventCodec`)。バックエンド`PracticeWebSocketHandler.handlePartialTranscriptRequest`は本番の`ATTR_AUDIO`バッファを**リセットせず**スナップショットを取って`sttClient.transcribe`にかけるため、録音自体は継続され`end_turn`時の本文字起こしに影響しない。同一セッションで前回のプレビュー処理がまだ完了していない場合は`AtomicBoolean`(`ATTR_PARTIAL_IN_PROGRESS`)で無視する(クライアント側は多重送信を気にせず一定間隔で送るだけでよい)ほか、専用の`partialTranscriptExecutor`(2スレッド)で実行しWSハンドラのメッセージ処理をブロックしない。フロントは録音停止後(`processing`中、確定結果`transcript`到着まで)も直前のプレビューを表示し続け、確定結果が届いたらそちらを優先表示することでキャプションが一瞬空白に戻るのを防いでいる。録音のたびに再文字起こしする分バックエンド負荷は増えるが、ユーザーが明示的にこの方式を選択した(簡易な「聞き取り中...」プレースホルダー案は不採用)
- **STTのハルシネーション対策**: `WhisperSttClient`は`vad_filter=true`を必ず付けてfaster-whisper-server(Speaches)を呼ぶ。無音/低音量区間をそのままWhisperに渡すと、学習データ由来の無関係なフレーズを延々と繰り返す既知の幻覚が起きるため、VAD(音声区間検出)で無音を除去してから文字起こしさせる
- **回答フレームワーク(STAR/PREP)**: `PracticePromptFactory`で、経験を聞く質問にはSTAR型(状況→課題→行動→結果)、自己PR・志望動機・逆質問にはPREP型(結論→理由→具体例→結論)をコツ・回答例の型として使うよう指示している(型の名前は言わず、型に沿った内容だけ自然に話す)
- **話し方だけでなく回答内容の妥当性も確認する**: 「入社後に挑戦したいこと」等の質問で、ユーザーの回答が会社の実際の事業内容と食い違っている(または根拠の乏しい主張のみ)のに、コーチが話し方・構成の改善だけを提案してその中身をそのまま磨こうとしてしまう問題があった。`PracticePromptFactory`に「回答内容の妥当性を確認する」セクションを追加し、①企業情報(概要・ソース)と回答内容が食い違っていれば話し方のコツより先にその点を深掘り質問で気づかせる、②企業情報に無い内容は断定せず事実確認の姿勢で尋ねる(捏造されたユーザーの経歴の扱いと同じ方針)、③企業情報の有無に関わらず具体的な理由・根拠が伴わない主張は深掘りで具体化を促す、という3点を明示した
- **音声の話速・フィラーワード・間をLLMへの参考情報として渡す**: `WhisperSttClient`は`response_format=verbose_json`でSTTを呼び、発話全体の長さ(`duration`)とVAD後の発話区間(`segments`)を`TranscriptionResult`として受け取る(タイムスタンプ非対応のSTTサーバーでは`duration`がnullになり、以降の分析はスキップされるだけで従来通り動く)。`SpeechMetricsAnalyzer`がテキストと`duration`から話速(1秒あたりの文字数、日本語の目安で普通6〜7・早口9〜10・遅口5弱を元にSLOW/NORMAL/FAST判定)・フィラーワード数(「えーと」「あの」等の簡易パターンマッチ、完全な検出ではない)・`segments`間の最大の間(秒)を算出する。いずれもLLM(音声非対応のテキストモデル)には直接聞かせられないため、"Speech Description Method"(音声特徴を自然言語の説明に変換してプロンプトに含める手法)に倣い、`PracticeTurnService.withSpeechNote`が`[話し方の参考情報(システムが自動計測。ユーザー自身の発言ではない)]: ...`という注記をLLMへの入力(`userMessage`)にだけ付記する。**注意**: `chat_messages.content`への保存やフロントへの`transcript`イベントには一切含めない(ユーザーには見せない裏側の情報)。目立った特徴(速い/遅い/フィラー2回以上/間2秒以上)が無ければ`describe()`はnullを返し、注記自体を付けない(毎ターン言及されると煩わしいため)。`PracticePromptFactory`側にもこのメモの解釈方法(内容に課題がある時だけ1文以内でさりげなく触れる、メモの文言や「計測によると」等の種明かしをしない)を指示している
- **業界情報はデフォルトで企業概要に含める**: チャット中にボタンでオン/オフして都度検索する方式ではなく、企業リサーチ時に`companies.overview`へ業界動向も一緒に含める設計にした(検索クエリに「業界動向」を追加、`CompanyResearchPromptFactory`に業界動向を1〜2点含める指示)。理由: リアルタイム会話中のツール呼び出しは「ツール呼び出しを使わない」という既存方針に反し、TTS遅延対策で削った待ち時間を増やしてしまうため。既に確立した「検索→要約→保存→プロンプトに含める」パターンを再利用している
- **質問を切り替える際は前のセッションを必ず片付ける**: `usePracticeSession.ts`の`openQuestion`は、サイドバーで別の質問(または同じ質問の再オープン)をクリックしたときに呼ばれるが、開始前に必ず`teardownSocket()`(前のWebSocketを閉じる・`partialTranscript`ポーリングを止める・再生中の音声を止める)を呼び、`chatState`/`partialTranscript`を`idle`/空文字にリセットしてから新しいセッションを開始する。**注意**: これを怠ると、会話の途中(`chatState`が`processing`/`responding`/`recording`のまま)で別の質問に切り替えたときに、新しい画面がその残留状態を引き継いでマイクボタンが最初から`disabled`になり、何か別のイベントで偶然`idle`に戻るまで1回目のクリックが効かない(体感上「マイクボタンを2回押さないと録音/文字起こしが始まらない」)不具合を一度踏んだ。加えて前のWebSocketを閉じずに新しいものを繋ぐと、古いソケットのハンドラが新しいセッションと共有された`setChatState`/`setMessages`等を後から書き換えてしまう問題もあった
- **プロンプトは否定形の指示より肯定形の指示を優先する**: `PracticePromptFactory`/`CompanyResearchPromptFactory`のシステムプロンプトを、否定形の禁止指示(「〜してはいけない」「〜は書かない」「〜を避ける」)中心の書き方から、肯定形の指示(「〜します」「〜だけを書く」)中心の書き方に見直した。理由: LLM(特に本アプリで使っているような小型モデル)は否定命令文の処理が苦手で、禁止したい概念自体を先に処理してしまい、かえってその概念を出力してしまう("Pink Elephant Problem")ことが知られている。特に「以下にまとめます、のような前置きは書かない」のように悪い例の文言をそのまま引用する書き方は、その文言自体を模倣して出力するリスクを上げるため、悪い例の引用ではなく肯定形の指示(「1文字目から本文を書き始める」)に置き換えた。ADVICEマーカーの指示も、旧版は同じマーカー文字列を「付けてください/付けないでください」の2回登場させていたが、後者を「(通常時は)何も付けずそのまま話し始めてください」という肯定形に変え、マーカー文字列自体を1回しか見せないようにした(繰り返し提示するとその文字列が誤って出力される確率が上がるため)。良い例/悪い例のペア提示(few-shot)や出力例の提示は、小型モデルへの効果が高い技術として確認済みのため維持・強化している
- **プロンプトは「誰の情報か」をタグで構造的に分離する**: `PracticePromptFactory.systemPrompt`/`MockInterviewPromptFactory.followUpSystemPrompt`のシステムプロンプトを、`<候補者情報>`(ユーザーが事前に登録した参考情報。下記のユーザープロフィール機能)・`<会社情報>`(mockモードでは`<会社情報>`、practiceモードでは`<企業情報>`)・`<あなたの役割>`(面接官/コーチとしての振る舞いルール)の3つのタグで明確に区切る構成にした。理由: 文章を地続きに書くと、候補者について書かれた背景情報と、AI自身が話すべき内容や役割指示が、小型モデルにとって曖昧になりやすい(実際に、自己紹介で述べられた活動を面接官が唐突に自分の話であるかのように語り出す・登録していない情報を断定的に話すといった不具合が起きた)。候補者情報ブロックの中身は常に「参考情報として読むだけで、あなた自身の経験や意見であるかのように話さない」という指示を`<あなたの役割>`内に明記している。各タグは未登録でも常に出力し、中身を「(登録されていません)」にする(タグの有無で構造が変わるとかえって小型モデルが混乱しやすいため、構造は常に一定にする)
- **ユーザープロフィール機能(履歴書のような参考情報)**: `com.interviewapp.profile`パッケージ。ローカル単一ユーザー前提でログイン・複数ユーザーの概念は持たず、`user_profile`テーブル(`V6__add_user_profile.sql`)は常に高々1行しか使わない(`UserProfileRepository.findFirstByOrderByCreatedAtAsc`)。`UserProfileService.getResumeTextOrNull()`が未登録/空文字ならnullを返し、`PracticeTurnService`/`MockTurnService`がターンごとにこれを取得して上記の`<候補者情報>`タグに渡す(DBアクセスを避けるためのキャッシュ等は行わず、都度取得する単純な実装。1ターンあたりの追加コストは無視できる)。フロントはサイドバー左下の「設定」ボタン(`Sidebar.tsx`、以前は未実装のダミーボタンだった)から`UserSettingsModal.tsx`を開き、`GET`/`PUT /api/profile`で1つのフリーテキストの`textarea`を編集する(会社概要と同じ「まずはフリーテキスト1本」の設計方針。項目を細かく構造化するのは実際に狭さを感じてから検討する)
- ローカル1GPU環境では `OLLAMA_NUM_PARALLEL` を上げてもVRAM不足でキューイングされるだけのことが多い。並列化を前提にした設計をしない

## コーディング規約

- Java: DTOは可能な限り `record` を使う。Controllerは薄く保ち、ビジネスロジックは `Service` に置く。インデントは4スペース
- Spring AIのツールは `@Tool` アノテーションで定義し、構造化出力が必要な場合は `.entity(Xxx.class)` を使う
- TypeScript: strict mode。コンポーネントは関数コンポーネント＋Hooksのみ。インデントは2スペース
- 命名: DBカラムはsnake_case、Java/TypeScriptはcamelCase(JPAのマッピングで変換する)

## テスト方針

- **Backend**: JUnit 5 + Mockitoでユニットテスト(Service層中心)。Testcontainers(`@ServiceConnection`)で実物のPostgresを使った統合テストを書く(H2での代替は不可、本番と挙動が変わるため)。Controllerは`@WebMvcTest`+MockMvcでスライステストにする
- **Frontend**: Vitest + React Testing Libraryでコンポーネント/フックのユニットテスト。Playwrightで主要フロー(1問の練習を開始→回答→終了まで)のE2Eテスト
- マーカー検出(`<<ADVICE>>`、`<<QUESTION_DONE>>`)のようなパース処理は、WebSocketハンドラから切り離した純粋関数/クラスとして実装し、単体テストしやすくする
- Ollama/faster-whisper/VOICEVOXへの実際のHTTP呼び出しはテストではモック/スタブに差し替える。実サービスに依存する統合テストは書かない(ローカル専用サービスでCI環境に存在しないため)
- 新しい機能を実装したら、同じタイミングで対応するテストも書く。最低限: Service層のユニットテスト1つ以上。分岐ロジック(マーカー検出、終了判定、メッセージ種別判定など)は正常系・異常系を個別にテストする

## 現在の状況

- 練習モード: REST API + WebSocket チャット(Ollama/STT/TTS)+ フロント実装まで完了。フロントUIは `ui-design/` のプロトタイプに合わせて刷新済み
- 企業リサーチ(会社追加ウィザード): 着手中。Web検索はSearXNG自己ホストで実装済み、`companies.overview` に集約、LLMで要約・質問生成
- 本番モード(本番模擬面接): 質問ごとに独立した一問一答形式で、バックエンド(REST API + WebSocket + レポート生成)+ フロント(各質問のチャット画面右上の「本番を開始」ボタン、面接画面・結果画面)まで実装済み・テスト済み
- 汎用的な質問集: サイドバーの「会社」セクションの上に、既存の質問バンクを複製した会社非依存の質問一覧を表示。練習・本番とも会社の質問と同じ仕組みで動く

## 今後追加したい機能(未着手・アイデアメモ)

ユーザーからの要望メモ。優先順位や設計は未確定、着手前に必ず相談する。

- **質問と回答が確定したらKeepに出力**: 練習チャットで固まった質問・回答のペアをGoogle Keepにエクスポートしたい。Google Keepには公式の一般公開APIが無い(Keep APIはGoogle Workspaceアカウント向けの制限付きAPI)ため、実現方法(Workspace API利用 or クリップボード/Markdownファイルへのエクスポートで代替 等)を先に調査する必要がある
- **ユーザー設定で汎用的な質問への回答・経験を登録**: 自己PRや志望動機など使い回せる経験をユーザープロフィールとして登録し、練習チャットのプロンプトに反映する(ユーザープロフィール機能自体は実装済み。ここでの要望はさらに質問ごとの回答例まで踏み込んで登録したい、という話だと推測されるため、着手前に要件を確認する)

### 気づいている他のやるべきこと

- **既存会社の概要再生成の導線が無い**: 「ソースを管理」で複数URLを追加できるようになったが、それを使って会社概要を再生成→保存するボタン/APIがまだ無い(今は手動でPATCHするか、ステートレスな `/research` を都度呼ぶしかない)
- **devcontainerで `OLLAMA_MODEL` が転送されない**: `docker-compose.dev.yml` の `devcontainer` サービスの `environment` に `OLLAMA_MODEL` が無く、`.env` で上書きしても効かない。今は `application.yml` のデフォルト値自体を変える運用でしのいでいる
- **Spring AIのバージョンが古い(1.0.0-M5)**: 最新は2.0.0まで進んでいる。アップグレードすればOllamaの `think` フィールドをネイティブに使えるようになり、自前の `OllamaChatClient` が不要になる可能性があるが、破壊的変更を伴うため計画的に検証が必要
- **関連ソース選定ロジック(文字bi-gram)の精度**: 練習チャットでどのソースを参考にするか選ぶロジックが素朴な文字一致ベース。件数が増えてきたら埋め込みベースの検索に置き換える余地がある
- **質問バンクの拡充**: 現状はキュレーションした静的データ(既存24件+本番用汎用質問5件、`JOB_AXIS`カテゴリ新設)のみ。実際の練習・本番で狭すぎる/偏っていると感じたら追加・調整する
- **VOICEVOX Engineのネイティブストリーミング合成待ち**: 実装されれば1文単位でのWAV待ちすら不要になり、さらに遅延を減らせる見込み(`voicevox_engine` issue #1492 が実装されたら追従を検討)
- **TTS失敗時のテキスト/音声同期のズレ**: 文単位のTTS合成が失敗すると、その文の音声だけ届かず、以降の文の音声と表示テキストの対応が1つずつズレる(最終的に全文は正しく表示されるが、音声との同期がその回だけ崩れる)。バイナリ音声フレームに文の対応関係を示すメタデータが無いのが原因。頻度が低ければ許容、気になるようならWSプロトコルの拡張を検討
- **練習モードのシステムプロンプトが長くなると小型モデルで劣化する可能性**: `PracticeTurnService.RELEVANT_SOURCE_LIMIT`(3件)×`PracticePromptFactory.SOURCE_CONTENT_LIMIT`(1500文字)で、関連ソースだけで最大4500文字(日本語で概ね2000〜3000トークン超)がシステムプロンプトに載る可能性があり、これに会社概要・会話履歴が加わると、小型モデルで指示追従が劣化し始めるとされる目安(数千トークン程度)に近づく。現状は`num_ctx`(8192)には収まるため動作はするが、体感で「指示を無視される」「関係ない話をする」等が増えるようなら、関連ソース件数や1件あたりの文字数を減らす方向で調整する
- **フィラーワード検出は簡易な文字列一致で完全ではない**: `SpeechMetricsAnalyzer`のフィラーワード検出(「えーと」「あの」等)は正規表現による大まかな目安で、文脈を見た厳密な検出ではない(例えば「あの」は指示語としても使われるため、検出対象からは意図的に除外している)。話速の閾値(1秒あたり5.0/8.5文字)も一般的な目安を採用しただけで、実際の練習で違和感があれば調整する
- **本番モードは面接の途中でリロード/再接続すると続きに戻れない**: `useMockSession`はセッション開始時の対象質問・進捗をReactのstateだけで保持しており、`GET /api/sessions/{sessionId}`(既存の`SessionDetail`、練習モードと共用)は`mock_session_questions`の内容を含まないため、ページを再読み込みすると"selecting"に戻ってしまう(セッション自体はサーバー側でIN_PROGRESSのまま残る)。一問一答形式への作り替え(前述)で質問が常に1問になったため以前より影響は小さいが、深掘りの途中経過(フォローアップ回数等)は失われる。本番専用の詳細取得エンドポイント(例: `GET /api/sessions/{id}/mock-detail`)を追加し、`useMockSession`起動時に再接続を試みる形にすると解消できる
- **本番モードのマーカーはストリーミングの恩恵を受けられない**: `<<QUESTION_DONE>>`は応答全体がマーカーそのもの(深掘りを続ける場合は応答全体が質問文になる)という想定のため、`MockTurnService`はLLM応答を全て受け取ってから初めてテキスト/TTSを送り始める(練習モードの`<<ADVICE>>`は冒頭マーカーで早期判定できるため、生成中から段階的に送れる)。本番の深掘り応答は短いので体感影響は小さい想定。なお、質問の提示・切り替え自体はLLMを呼ばずコード側が直接文言を送るため、この制約を受けない
